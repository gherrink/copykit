#!/bin/sh

# Run tests first to ensure code functionality
echo "Running tests before commit..."
pnpm test || exit 1

# If tests pass, run lint-staged for linting and formatting  
echo "Tests passed. Running linting and formatting..."
npx lint-staged